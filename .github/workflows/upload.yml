name: Captain Upload API

on:
  workflow_dispatch:
    inputs:
      filename:
        required: true
      content:
        required: true

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
    - name: Upload file
      run: |
        echo "Uploading ${{ inputs.filename }}"
        echo "Captain API Ready"
